# Hashmap

If a key exists

```
unordered_map<int, int> counter;

// If key doesn't exist
if (counter.find(num) == counter.end()) {
  
}
```

Delete key

```
unordered_map<int, int> counter;

counter.erase(key);
```

